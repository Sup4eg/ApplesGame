#pragma once
#include <string>

namespace ApplesGame {

  const std::string RESOURCES_PATH = "Resources/";
  const int SCREEN_WIDTH = 800;
  const int SCREEN_HEIGTH = 600;

  const float SCREEN_X_MARGIN = 60.f;
  const float SCREEN_Y_MARGIN = 30.f;

  const std::string GAME_NAME = "Apples game!";
  const float INITIAL_SPEED = 100.f; // pixels per second
  const float ACCELERATION = 40.f;

  const float PLAYER_SIZE = 25.f;
  const float APPLES_SIZE = 25.f;
  const float ROCKS_SIZE = 25.f;

  const int NUM_APPLES = 30;
  const int NUM_ROCKS = 10;

  const float PAUSE_LENGTH = 3.f;

  //settings
  const float ARROW_WIDTH = 25.6f;
  const float ARROW_HEIGHT = 14.4f;

}